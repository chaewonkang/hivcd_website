import React from "react";
import "./BoardPostWrapper.css";

const BoardPostWrapper = ({ children }) => {
  return <div className="board_post_wrapper">{children}</div>;
};

export default BoardPostWrapper;
